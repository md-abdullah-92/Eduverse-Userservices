server:
  port: ${PORT:5000}

spring:
  application:
    name: user-services
  
  datasource:
    url: ${DATABASE_URL:jdbc:mysql://localhost:3306/eduverse}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:password}
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        
  mail:
    host: ${SMTP_HOST:smtp.gmail.com}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME:abdullahalmahadiapurbo@gmail.com}
    password: ${SMTP_PASSWORD:kmlbekkdfnfxwdfk}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:4aa034a48e45199a3aac91e16fd57b77a9a641cdc18311dbbb2e05673498aa9f}
  expiration: ${JWT_EXPIRATION:2592000000} # 30 days in milliseconds
  
# CORS Configuration
cors:
  allowed-origins: ${CORS_ORIGINS:http://localhost:3000,http://localhost:3001}
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  allow-credentials: true

# Logging
logging:
  level:
    com.eduverse: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
